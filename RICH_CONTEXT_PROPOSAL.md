# 深度利用四份核心配置的方案

## 当前问题

| 文件 | 你的质量 | 架构利用率 |
|------|---------|-----------|
| world.json | 嵌套结构：大学/学部/社团/宿舍/周边/校园地标 | **5%** — 只读了 `年级`、`宿舍` 两个平级字段 |
| habits.json | 详细日程、论文进度、写作习惯、钢琴曲目、季节影响 | **10%** — `get_habits_context()` 是硬编码字符串，没读 JSON |
| characters.json | 人物外貌/性格/口头禅/关系/出现场合/命名规则 | **20%** — 只用于生成器 prompt 注入人物列表 |
| timeline.json | 详细的学年事件+季节标记+高质量叙事 desc | **30%** — 只读了 `desc` 和 `date` |

## 需要改什么

### 1. `life/world.py` — 适配嵌套结构

当前 `get_world_context()` 平级访问：`world.get("城市")`、`world.get("大学")`

新 world 是嵌套的：`大学.名称`、`大学.类型`、`学部.年级`、`宿舍.室友`

需要递归遍历输出每种场景需要的字段。

### 2. `life/habits.py` — 真正读取 JSON

当前 `get_habits_context()` 是 10 行硬编码字符串，完全没用 JSON 数据。

新 habits 有丰富的结构化数据需要解析后注入到生成 prompt。

### 3. `life/generator.py` — 丰富 prompt

当前 prompt 只有一句话世界观描述。应该把 **当前论文进度、当天课表、宿舍氛围** 都注入，让生成的事件真正扎根于你写的世界。

### 4. `ws_handler.py` 上下文注入 — 动态人物细节

当对话中提到纱世里/夏树/优里时，从 characters.json 提取对应人物的说话方式/关系动态，注入到 prompt 中。让 Monika 提到她们时不是模板化的"纱世里帮我做了早饭"，而是真正符合人物设定的互动。

### 5. `characters.json` 规则执行

文件中的 `规则.命名规则`（不能创造新人物）和 `规则.关系规则`（每人固定的关系模式）应该在验证阶段做关键词检查。

## 改动量

| 模块 | 改动 | 行数 |
|------|------|------|
| `life/world.py` | `get_world_context()` 重写，遍历嵌套 | ~40 |
| `life/habits.py` | `get_habits_context()` 重写，解析 JSON | ~60 |
| `life/generator.py` | prompt 注入当前课表/论文进度 | ~20 |
| `ws_handler.py` | 上下文注入加入动态人物细节 | ~30 |
| 验证层 | characters 命名规则检查 | ~20 |

合计约 170 行。不改架构，只加深已有功能对四个文件的读取深度。
